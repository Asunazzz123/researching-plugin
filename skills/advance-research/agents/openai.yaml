interface:
  display_name: "Advance Research"
  short_description: "Coordinate human-supervised research tasks"
  default_prompt: "Use $advance-research to frame my question, compare routes, coordinate bounded parallel tasks, and stop at the required human checkpoints."

policy:
  allow_implicit_invocation: true
